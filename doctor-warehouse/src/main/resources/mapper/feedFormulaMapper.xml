<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="FeedFormula">
    <resultMap id="FeedFormulaMap" type="FeedFormula">
        <id column="id" property="id"/>
        <result column="feed_id" property="feedId"/>
        <result column="feed_name" property="feedName"/>
        <result column="farm_id" property="farmId"/>
        <result column="farm_name" property="farmName"/>
        <result column="formula" property="formula"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        doctor_feed_formulas
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        feed_id, feed_name, farm_id, farm_name, formula, created_at, updated_at
    </sql>

    <sql id="vals">
        #{feedId}, #{feedName}, #{farmId}, #{farmName}, #{formula}, now(), now()
    </sql>

    <insert id="create" parameterType="FeedFormula" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="FeedFormulaMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <select id="findByFeedIdAndFarmId" parameterType="map" resultMap="FeedFormulaMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE feed_id = #{feedId} and farm_id = #{farmId}
    </select>

    <update id="update" parameterType="FeedFormula">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="feedId != null">feed_id = #{feedId},</if>
            <if test="feedName != null">feed_name = #{feedName},</if>
            <if test="farmId != null">farm_id = #{farmId},</if>
            <if test="farmName != null">farm_name = #{farmName},</if>
            <if test="formula != null">formula = #{formula},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>
</mapper>