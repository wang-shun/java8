<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorWareHouseTrack">

    <resultMap id="DoctorWareHouseTrackMap" type="DoctorWareHouseTrack">

        <result column="ware_house_id" property="wareHouseId"/>
        <result column="farm_id" property="farmId"/>
        <result column="farm_name" property="farmName"/>
        <result column="manager_id" property="managerId"/>
        <result column="manager_name" property="managerName"/>
        <result column="material_lot_number" property="materialLotNumber"/>
        <result column="lot_number" property="lotNumber"/>
        <result column="is_default" property="isDefault"/>
        <result column="extra" property="extra"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    
    </resultMap>

    <sql id="tb"> doctor_ware_house_tracks </sql>

    <sql id="cols_all">
      id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        `ware_house_id`,`farm_id`,`farm_name`,`manager_id`,`manager_name`,`material_lot_number`,`lot_number`,`is_default`,`extra`,`created_at`,`updated_at`
    </sql>

    <sql id="vals">
        #{wareHouseId},#{farmId},#{farmName},#{managerId},#{managerName},#{materialLotNumber},#{lotNumber},#{isDefault},#{extra},now(),now()
    </sql>

    <insert id="create" parameterType="DoctorWareHouseTrack" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO <include refid="tb" />
        (<include refid="cols_exclude_id" />)
            VALUES (<include refid="vals" />)
    </insert>

    <insert id="creates" parameterType="list">
        INSERT INTO <include refid="tb"/>
        ( <include refid="cols_exclude_id"/> )
        VALUES
        <foreach collection="list" item="i" separator=",">
        (#{i.wareHouseId},#{i.farmId},#{i.farmName},#{i.managerId},#{i.managerName},#{i.materialLotNumber},#{i.lotNumber},#{i.isDefault},#{i.extra},now(),now())
        </foreach>
    </insert>

    <delete id="delete" parameterType="Long">
        DELETE FROM <include refid="tb"/> WHERE
        id=#{id}
    </delete>

    <select id="findById" parameterType="long" resultMap="DoctorWareHouseTrackMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>


    <update id="update" parameterType="DoctorWareHouseTrack">
        UPDATE <include refid="tb"/>
        <set>
        updated_at = now()
        <if test="wareHouseId != null" > ,`ware_house_id` = #{wareHouseId}</if>
        <if test="farmId != null" > ,`farm_id` = #{farmId}</if>
        <if test="farmName != null" > ,`farm_name` = #{farmName}</if>
        <if test="managerId != null" > ,`manager_id` = #{managerId}</if>
        <if test="managerName != null" > ,`manager_name` = #{managerName}</if>
        <if test="materialLotNumber != null" > ,`material_lot_number` = #{materialLotNumber}</if>
        <if test="lotNumber != null" > ,`lot_number` = #{lotNumber}</if>
        <if test="isDefault != null" > ,`is_default` = #{isDefault}</if>
        <if test="extra != null" > ,`extra` = #{extra}</if>
        <if test="createdAt != null" > ,`created_at` = #{createdAt}</if>
        </set>
        WHERE id=#{id}
    </update>
</mapper>