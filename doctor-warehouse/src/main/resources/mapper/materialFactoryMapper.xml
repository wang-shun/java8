<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="MaterialFactory">
    <resultMap id="MaterialFactoryMap" type="MaterialFactory">
        <id column="id" property="id"/>
        <result column="farm_id" property="farmId"/>
        <result column="farm_name" property="farmName"/>
        <result column="factory_name" property="factoryName"/>
        <result column="extra" property="extra"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        doctor_material_factorys
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        farm_id, farm_name, factory_name, extra, created_at, updated_at
    </sql>

    <sql id="vals">
        #{farmId}, #{farmName}, #{factoryName}, #{extra}, now(), now()
    </sql>

    <insert id="create" parameterType="MaterialFactory" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="MaterialFactoryMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <select id="findByFarmAndName" parameterType="map" resultMap="MaterialFactoryMap">
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE farm_id = #{farmId} and factory_name = #{factoryName}
    </select>

    <select id="findByFarmId" parameterType="long" resultMap="MaterialFactoryMap">
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE farm_id = #{farmId}
    </select>

    <update id="update" parameterType="MaterialFactory">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="farmId != null">farm_id = #{farmId},</if>
            <if test="farmName != null">farm_name = #{farmName},</if>
            <if test="factoryName != null">factory_name = #{factoryName},</if>
            <if test="extra != null">extra = #{extra},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>
</mapper>