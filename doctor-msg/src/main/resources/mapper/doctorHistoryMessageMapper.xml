<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorHistoryMessage">
    <resultMap id="DoctorHistoryMessageMap" type="DoctorHistoryMessage">
        <id column="id" property="id"/>
        <result column="farm_id" property="farmId"/>
        <result column="rule_id" property="ruleId"/>
        <result column="role_id" property="roleId"/>
        <result column="template_id" property="templateId"/>
        <result column="template_name" property="templateName"/>
        <result column="business_id" property="businessId"/>
        <result column="message_template" property="messageTemplate"/>
        <result column="type" property="type"/>
        <result column="event_type" property="eventType"/>
        <result column="category" property="category"/>
        <result column="data" property="data"/>
        <result column="content" property="content"/>
        <result column="url" property="url"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="relMessageId" property="rel_message_id"/>
    </resultMap>

    <sql id="tb">
        doctor_history_messages
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        farm_id, rule_id, role_id, template_id, template_name, business_id, message_template, type, event_type, category, data, content ,url, created_by, created_at, updated_at, relMessageId
    </sql>

    <sql id="vals">
        #{farmId}, #{ruleId}, #{roleId}, #{templateId}, #{templateName}, #{businessId}, #{messageTemplate}, #{type}, #{eventType},  #{category}, #{data}, #{content}, #{url}, #{createdBy}, now(), now(), rel_message_id
    </sql>

    <insert id="create" parameterType="DoctorHistoryMessage" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <insert id="creates" parameterType="list">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        <foreach collection="list" item="i" separator=",">
            (#{i.farmId}, #{i.ruleId}, #{i.roleId}, #{i.templateId}, #{i.templateName}, #{i.businessId}, #{i.messageTemplate}, #{i.type}, #{i.eventType},#{i.category}, #{i.data}, #{i.content}, #{i.url} , #{i.createdBy}, now(), now(), #{i.relMessageId})
        </foreach>
    </insert>

</mapper>