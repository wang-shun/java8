<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorMessageRule">
    <resultMap id="DoctorMessageRuleMap" type="DoctorMessageRule">
        <id column="id" property="id"/>
        <result column="farm_id" property="farmId"/>
        <result column="template_id" property="templateId"/>
        <result column="template_name" property="templateName"/>
        <result column="type" property="type"/>
        <result column="category" property="category"/>
        <result column="rule_value" property="ruleValue"/>
        <result column="use_default" property="useDefault"/>
        <result column="status" property="status"/>
        <result column="describe" property="describe"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        doctor_message_rules
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        farm_id, template_id, template_name, type, category, rule_value, use_default, status, `describe`, created_at, updated_at
    </sql>

    <sql id="vals">
        #{farmId}, #{templateId}, #{templateName}, #{type}, #{category}, #{ruleValue}, #{useDefault}, #{status}, #{describe}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorMessageRule" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorMessageRuleMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorMessageRule">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="farmId != null">farm_id = #{farmId},</if>
            <if test="templateId != null">template_id = #{templateId},</if>
            <if test="templateName != null">template_name = #{templateName},</if>
            <if test="type != null">type = #{type},</if>
            <if test="category != null">category = #{category},</if>
            <if test="ruleValue != null">rule_value = #{ruleValue},</if>
            <if test="useDefault != null">use_default = #{useDefault},</if>
            <if test="status != null">status = #{status},</if>
            <if test="describe != null">`describe` = #{describe},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <sql id="criteria">
        <where>
            <if test="farmId != null">AND farm_id = #{farmId}</if>
            <if test="templateId != null">AND template_id = #{templateId}</if>
            <if test="templateName != null">AND template_name = #{templateName}</if>
            <if test="type != null">AND type = #{type}</if>
            <if test="category != null">AND category = #{category}</if>
            <if test="useDefault != null">AND use_default = #{useDefault}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </sql>

    <select id="list" parameterType="map" resultMap="DoctorMessageRuleMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        <include refid="criteria"/>
    </select>

    <!-- 获取猪场和规则关系 -->
    <select id="findByTplAndFarm" parameterType="map" resultMap="DoctorMessageRuleMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE farm_id = #{farmId} AND template_id = #{templateId} AND status != -1
    </select>

    <!-- 获取猪场和规则关系 -->
    <select id="findByTpl" parameterType="long" resultMap="DoctorMessageRuleMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE template_id = #{templateId} AND status != -1
    </select>

    <select id="findByFarmId" parameterType="long" resultMap="DoctorMessageRuleMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE farm_id = #{farmId} AND status != -1
    </select>

    <select id="findByFarmIdAndTemplateName" parameterType="map" resultMap="DoctorMessageRuleMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE farm_id = #{farmId} AND status != -1 AND template_name LIKE CONCAT('%',#{templateName},'%'}
    </select>

</mapper>