<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorMessageRule">
    <resultMap id="DoctorMessageRuleMap" type="DoctorMessageRule">
        <result column="farm_id" property="farmId"/>
        <result column="template_id" property="templateId"/>
        <result column="template_name" property="templateName"/>
        <result column="type" property="type"/>
        <result column="category" property="category"/>
        <result column="rule_value" property="ruleValue"/>
        <result column="use_default" property="useDefault"/>
        <result column="status" property="status"/>
        <result column="describe" property="describe"/>
        <result column="channels" property="channels"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        doctor_message_rules
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        farm_id, template_id, template_name, type, category, rule_value, use_default, status, `describe`, channels, created_at, updated_at
    </sql>

    <sql id="vals">
        #{farmId}, #{templateId}, #{templateName}, #{type}, #{category}, #{ruleValue}, #{useDefault}, #{status}, #{describe}, #{channels}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorMessageRule" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorMessageRuleMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorMessageRule">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="farmId != null">farm_id = #{farmId},</if>
            <if test="templateId != null">template_id = #{templateId},</if>
            <if test="templateName != null">template_name = #{templateName},</if>
            <if test="type != null">type = #{type},</if>
            <if test="category != null">category = #{category},</if>
            <if test="ruleValue != null">rule_value = #{ruleValue},</if>
            <if test="useDefault != null">use_default = #{useDefault},</if>
            <if test="status != null">status = #{status},</if>
            <if test="describe != null">`describe` = #{describe},</if>
            <if test="channels != null">channels = #{channels},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

</mapper>