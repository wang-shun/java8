<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorMessageRule">
    <resultMap id="DoctorMessageRuleMap" type="DoctorMessageRule">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="rule_type" property="ruleType"/>
        <result column="rule_value" property="ruleValue"/>
        <result column="rule_start_value" property="ruleStartValue"/>
        <result column="rule_end_value" property="ruleEndValue"/>
        <result column="describe" property="describe"/>
        <result column="status" property="status"/>
        <result column="template_name" property="templateName"/>
        <result column="content" property="content"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        doctor_message_rules
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        name, type, rule_type, rule_value, rule_start_value, rule_end_value, describe, status, template_name, content, created_at, updated_at
    </sql>

    <sql id="vals">
        #{name}, #{type}, #{ruleType}, #{ruleValue}, #{ruleStartValue}, #{ruleEndValue}, #{describe}, #{status}, #{templateName}, #{content}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorMessageRule" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorMessageRuleMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorMessageRule">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="type != null">type = #{type},</if>
            <if test="ruleType != null">rule_type = #{ruleType},</if>
            <if test="ruleValue != null">rule_value = #{ruleValue},</if>
            <if test="ruleStartValue != null">rule_start_value = #{ruleStartValue},</if>
            <if test="ruleEndValue != null">rule_end_value = #{ruleEndValue},</if>
            <if test="describe != null">describe = #{describe},</if>
            <if test="status != null">status = #{status},</if>
            <if test="templateName != null">template_name = #{templateName},</if>
            <if test="content != null">content = #{content},</if>
            
            
            
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>
</mapper>