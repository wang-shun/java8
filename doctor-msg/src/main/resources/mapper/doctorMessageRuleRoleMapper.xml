<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorMessageRuleRole">
    <resultMap id="DoctorMessageRuleRoleMap" type="DoctorMessageRuleRole">
        <id column="id" property="id"/>
        <result column="rule_id" property="ruleId"/>
        <result column="template_id" property="templateId"/>
        <result column="farm_id" property="farmId"/>
        <result column="role_id" property="roleId"/>
        <result column="rule_value" property="ruleValue"/>
        <result column="use_default" property="useDefault"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        doctor_message_rule_roles
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        rule_id, template_id, farm_id, role_id, rule_value, use_default, created_at, updated_at
    </sql>

    <sql id="vals">
        #{ruleId}, #{templateId}, #{farmId}, #{roleId}, #{ruleValue}, #{useDefault}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorMessageRuleRole" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorMessageRuleRoleMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorMessageRuleRole">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="ruleId != null">rule_id = #{ruleId},</if>
            <if test="templateId != null">template_id = #{templateId},</if>
            <if test="farmId != null">farm_id = #{farmId},</if>
            <if test="roleId != null">role_id = #{roleId},</if>
            <if test="ruleValue != null">rule_value = #{ruleValue},</if>
            <if test="useDefault != null">use_default = #{useDefault},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="findByTplId" parameterType="long" resultMap="DoctorMessageRuleRoleMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE template_id = #{templateId}
    </select>



</mapper>