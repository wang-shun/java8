<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorMessageRuleRole">
    <resultMap id="DoctorMessageRuleRoleMap" type="DoctorMessageRuleRole">
        <id column="id" property="id"/>
        <result column="role_id" property="roleId"/>
        <result column="rule_id" property="ruleId"/>
        <result column="type" property="type"/>
        <result column="rule_type" property="ruleType"/>
        <result column="rule_value" property="ruleValue"/>
        <result column="rule_start_value" property="ruleStartValue"/>
        <result column="rule_end_value" property="ruleEndValue"/>
        <result column="channel" property="channel"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        doctor_message_rule_roles
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        role_id, rule_id, type, rule_type, rule_value, rule_start_value, rule_end_value, channel, created_at, updated_at
    </sql>

    <sql id="vals">
        #{roleId}, #{ruleId}, #{type}, #{ruleType}, #{ruleValue}, #{ruleStartValue}, #{ruleEndValue}, #{channel}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorMessageRuleRole" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorMessageRuleRoleMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorMessageRuleRole">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="roleId != null">role_id = #{roleId},</if>
            <if test="ruleId != null">rule_id = #{ruleId},</if>
            <if test="type != null">type = #{type},</if>
            <if test="ruleType != null">rule_type = #{ruleType},</if>
            <if test="ruleValue != null">rule_value = #{ruleValue},</if>
            <if test="ruleStartValue != null">rule_start_value = #{ruleStartValue},</if>
            <if test="ruleEndValue != null">rule_end_value = #{ruleEndValue},</if>
            <if test="channel != null">channel = #{channel},</if>
            
            
            
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>
</mapper>