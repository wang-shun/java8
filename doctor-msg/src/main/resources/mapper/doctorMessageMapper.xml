<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorMessage">
    <resultMap id="DoctorMessageMap" type="DoctorMessage">
        <id column="id" property="id"/>
        <result column="farm_id" property="farmId"/>
        <result column="rule_id" property="ruleId"/>
        <result column="role_id" property="roleId"/>
        <result column="user_id" property="userId"/>
        <result column="template_id" property="templateId"/>
        <result column="type" property="type"/>
        <result column="category" property="category"/>
        <result column="data" property="data"/>
        <result column="channel" property="channel"/>
        <result column="url" property="url"/>
        <result column="status" property="status"/>
        <result column="sended_at" property="sendedAt"/>
        <result column="failed_by" property="failedBy"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        doctor_messages
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        farm_id, rule_id, role_id, user_id, template_id, type, category, data, channel, url, status, sended_at, failed_by, created_by, created_at, updated_at
    </sql>

    <sql id="vals">
        #{farmId}, #{ruleId}, #{roleId}, #{userId}, #{templateId}, #{type}, #{category}, #{data}, #{channel}, #{url}, #{status}, now(), #{failedBy}, #{createdBy}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorMessage" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <insert id="creates" parameterType="list">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        <foreach collection="list" item="i" separator=",">
            (#{i.farmId}, #{i.ruleId}, #{i.roleId}, #{i.userId}, #{i.templateId}, #{i.type}, #{i.category}, #{i.data}, #{i.channel}, #{i.status}, now(), #{i.failedBy}, #{createdBy}, now(), now())
        </foreach>
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorMessageMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorMessage">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="farmId != null">farm_id = #{farmId},</if>
            <if test="ruleId != null">rule_id = #{ruleId},</if>
            <if test="roleId != null">role_id = #{roleId},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="templateId != null">template_id = #{templateId},</if>
            <if test="type != null">type = #{type},</if>
            <if test="category != null">category = #{category},</if>
            <if test="data != null">data = #{data},</if>
            <if test="channel != null">channel = #{channel},</if>
            <if test="url != null">url = #{url},</if>
            <if test="status != null">status = #{status},</if>
            <if test="failedBy != null">failed_by = #{failedBy},</if>
            <if test="createdBy != null">created_by = #{createdBy},</if>
            sended_at=now(),
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <sql id="criteria">
        <where>
            <if test="farmId != null">AND farm_id = #{farmId}</if>
            <if test="ruleId != null">AND rule_id = #{ruleId}</if>
            <if test="roleId != null">AND role_id = #{roleId}</if>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="templateId != null">AND template_id = #{templateId}</if>
            <if test="type != null">AND type = #{type}</if>
            <if test="category != null">AND category = #{category}</if>
            <if test="channel != null">AND channel = #{channel}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </sql>

    <select id="list" resultMap="DoctorMessageMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        <include refid="criteria"/>
    </select>

</mapper>