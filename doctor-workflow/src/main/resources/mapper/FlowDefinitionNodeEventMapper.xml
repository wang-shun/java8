<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="FlowDefinitionNodeEvent">

    <resultMap id="FlowDefinitionNodeEventMap" type="FlowDefinitionNodeEvent">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="flow_definition_id" property="flowDefinitionId"/>
        <result column="source_node_id" property="sourceNodeId"/>
        <result column="handler" property="handler"/>
        <result column="expression" property="expression"/>
        <result column="target_node_id" property="targetNodeId"/>
        <result column="describe" property="describe"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        workflow_definition_node_events
    </sql>

    <sql id="cols_all">
        id,
        <include refid="cols_exclude_id"/>
    </sql>

    <sql id="cols_exclude_id">
        `name`,`flow_definition_id`,`source_node_id`,`handler`,`expression`,`target_node_id`,`describe`,`created_at`,`updated_at`    </sql>

    <sql id="vals">
        #{name},#{flowDefinitionId},#{sourceNodeId},#{handler},#{expression},#{targetNodeId},#{describe},now(),now()   </sql>

    <insert id="create" parameterType="FlowDefinitionNodeEvent" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb"/>
        (<include refid="cols_exclude_id"/>)
        VALUES
        (<include refid="vals"/>)
    </insert>

    <select id="findById" parameterType="int" resultMap="FlowDefinitionNodeEventMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE id = #{id}
    </select>


    <update id="update" parameterType="FlowDefinitionNodeEvent">
        UPDATE
        <include refid="tb"/>
        <set>
            update_at = now()
            <if test="name != null">,`name` = #{name}</if>
            <if test="flowDefinitionId != null">,`flow_definition_id` = #{flowDefinitionId}</if>
            <if test="sourceNodeId != null">,`source_node_id` = #{sourceNodeId}</if>
            <if test="handler != null">,`handler` = #{handler}</if>
            <if test="expression != null">,`expression` = #{expression}</if>
            <if test="targetNodeId != null">,`target_node_id` = #{targetNodeId}</if>
            <if test="describe != null">,`describe` = #{describe}</if>
            <if test="createdAt != null">,`created_at` = #{createdAt}</if>
        </set>
        WHERE id=#{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM
        <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <sql id="criteria">
        <where>
            <if test="id != null">AND `id` = #{id}</if>
            <if test="name != null">AND `name` = #{name}</if>
            <if test="flowDefinitionId != null">AND `flow_definition_id` = #{flowDefinitionId}</if>
            <if test="sourceNodeId != null">AND `source_node_id` = #{sourceNodeId}</if>
            <if test="targetNodeId != null">AND `target_node_id` = #{targetNodeId}</if>
            <if test="startAt != null">AND `created_at` >= #{startAt}</if>
            <if test="endAt != null">AND `created_at` &lt;= #{endAt}</if>
        </where>
    </sql>

    <select id="list" resultMap="FlowDefinitionNodeEventMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        <include refid="criteria"/>
        <if test="orderBy != null"> ORDER BY ${orderBy}</if>
        <if test="desc != null"> DESC </if>
    </select>

    <!-- 计数 -->
    <select id="count" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM <include refid="tb"/>
        <include refid="criteria"/>
    </select>

    <!-- 分页查询 -->
    <select id="paging" parameterType="map" resultMap="FlowDefinitionNodeEventMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        <include refid="criteria"/>
        <if test="orderBy != null"> ORDER BY ${orderBy}</if>
        <if test="desc != null"> DESC </if>
        LIMIT #{offset}, #{limit}
    </select>

</mapper>