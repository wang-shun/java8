<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="FlowProcess">

    <resultMap id="FlowProcessMap" type="FlowProcess">
        <id column="id" property="id"/>
        <result column="flow_definition_node_id" property="flowDefinitionNodeId"/>
        <result column="pre_flow_definition_node_id" property="preFlowDefinitionNodeId"/>
        <result column="flow_instance_id" property="flowInstanceId"/>
        <result column="flow_data" property="flowData"/>
        <result column="status" property="status"/>
        <result column="assignee" property="assignee"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        workflow_processes
    </sql>

    <sql id="cols_all">
        id,
        <include refid="cols_exclude_id"/>
    </sql>

    <sql id="cols_exclude_id">
        `flow_definition_node_id`,`pre_flow_definition_node_id`,`flow_instance_id`,`flow_data`,`status`,`assignee`,`created_at`,`updated_at`    </sql>

    <sql id="vals">
        #{flowDefinitionNodeId},#{preFlowDefinitionNodeId},#{flowInstanceId},#{flowData},#{status},#{assignee},now(),now()    </sql>

    <insert id="create" parameterType="FlowProcess" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb"/>
        (<include refid="cols_exclude_id"/>)
        VALUES
        (<include refid="vals"/>)
    </insert>

    <select id="findById" parameterType="int" resultMap="FlowProcessMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE id = #{id}
    </select>


    <update id="update" parameterType="FlowProcess">
        UPDATE
        <include refid="tb"/>
        <set>
            update_at = now()
            <if test="flowDefinitionNodeId != null">,`flow_definition_node_id` = #{flowDefinitionNodeId}</if>
            <if test="preFlowDefinitionNodeId != null">,`pre_flow_definition_node_id` = #{preFlowDefinitionNodeId}</if>
            <if test="flowInstanceId != null">,`flow_instance_id` = #{flowInstanceId}</if>
            <if test="flowData != null">,`flow_data` = #{flowData}</if>
            <if test="status != null">,`status` = #{status}</if>
            <if test="assignee != null">,`assignee` = #{assignee}</if>
            <if test="createdAt != null">,`created_at` = #{createdAt}</if>
        </set>
        WHERE id=#{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM
        <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <sql id="criteria">
        <where>
            <if test="id != null">AND `id` = #{id}</if>
            <if test="flowDefinitionNodeId != null">AND `flow_definition_node_id` = #{flowDefinitionNodeId}</if>
            <if test="flowInstanceId != null">AND `flow_instance_id` = #{flowInstanceId}</if>
            <if test="status != null">AND `status` = #{status}</if>
            <if test="assignee != null">AND `assignee` = #{assignee}</if>
            <if test="startAt != null">AND `created_at` >= #{startAt}</if>
            <if test="endAt != null">AND `created_at` &lt;= #{endAt}</if>
        </where>
    </sql>

    <select id="list" resultMap="FlowProcessMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        <include refid="criteria"/>
        <if test="orderBy != null"> ORDER BY ${orderBy}</if>
        <if test="desc != null"> DESC </if>
    </select>

    <!-- 计数 -->
    <select id="count" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM <include refid="tb"/>
        <include refid="criteria"/>
    </select>

    <!-- 分页查询 -->
    <select id="paging" parameterType="map" resultMap="FlowProcessMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        <include refid="criteria"/>
        <if test="orderBy != null"> ORDER BY ${orderBy}</if>
        <if test="desc != null"> DESC </if>
        LIMIT #{offset}, #{limit}
    </select>

</mapper>