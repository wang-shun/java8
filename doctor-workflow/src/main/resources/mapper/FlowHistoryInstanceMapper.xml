<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="FlowHistoryInstance">

    <resultMap id="FlowHistoryInstanceMap" type="FlowHistoryInstance">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="flow_definition_id" property="flowDefinitionId"/>
        <result column="flow_definition_key" property="flowDefinitionKey"/>
        <result column="business_id" property="businessId"/>
        <result column="business_data" property="businessData"/>
        <result column="status" property="status"/>
        <result column="type" property="type"/>
        <result column="operator_id" property="operatorId"/>
        <result column="operator_name" property="operatorName"/>
        <result column="parent_instance_id" property="parentInstanceId"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        workflow_history_process_instances
    </sql>

    <sql id="cols_all">
        id,
        <include refid="cols_exclude_id"/>
    </sql>

    <sql id="cols_exclude_id">
        `name`,`flow_definition_id`,`flow_definition_key`,`business_id`,`business_data`,`status`,`type`,`operator_id`,`operator_name`,`parent_instance_id`,`created_at`,`updated_at`    </sql>

    <sql id="vals">
        #{name},#{flowDefinitionId},#{flowDefinitionKey},#{businessId},#{businessData},#{status},#{type},#{operatorId},#{operatorName},#{parentInstanceId},now(),now()    </sql>

    <insert id="create" parameterType="FlowHistoryInstance" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb"/>
        (<include refid="cols_exclude_id"/>)
        VALUES
        (<include refid="vals"/>)
    </insert>

    <select id="findById" parameterType="int" resultMap="FlowHistoryInstanceMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="FlowHistoryInstance">
        UPDATE
        <include refid="tb"/>
        <set>
            update_at = now()
            <if test="name != null">,`name` = #{name}</if>
            <if test="flowDefinitionId != null">,`flow_definition_id` = #{flowDefinitionId}</if>
            <if test="flowDefinitionKey != null">,`flow_definition_key` = #{flowDefinitionKey}</if>
            <if test="businessId != null">,`business_id` = #{businessId}</if>
            <if test="businessData != null">,`business_data` = #{businessData}</if>
            <if test="status != null">,`status` = #{status}</if>
            <if test="type != null">,`type` = #{type}</if>
            <if test="operatorId != null">,`operator_id` = #{operatorId}</if>
            <if test="operatorName != null">,`operator_name` = #{operatorName}</if>
            <if test="parentInstanceId != null">,`parent_instance_id` = #{parentInstanceId}</if>
            <if test="createdAt != null">,`created_at` = #{createdAt}</if>
        </set>
        WHERE id=#{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM
        <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <delete id="deletes" parameterType="list">
        DELETE FROM
        <include refid="tb"/>
        WHERE id IN
        <foreach collection="list" open="(" separator="," close=")" item="item">
            #{item}
        </foreach>
    </delete>

    <sql id="criteria">
        <where>
            <if test="id != null">AND `id` = #{id}</if>
            <if test="name != null">AND `name` LIKE CONCAT('%',#{name},'%')</if>
            <if test="flowDefinitionId != null">AND `flow_definition_id` = #{flowDefinitionId}</if>
            <if test="flowDefinitionKey != null">AND `flow_definition_key` = #{flowDefinitionKey}</if>
            <if test="businessId != null">AND `business_id` = #{businessId}</if>
            <if test="status != null">AND `status` = #{status}</if>
            <if test="type != null">AND `type` = #{type}</if>
            <if test="operatorId != null">AND `operator_id` = #{operatorId}</if>
            <if test="operatorName != null">AND `operator_name` LIKE CONCAT('%',#{operatorName},'%')</if>
            <if test="parentInstanceId != null">AND `parent_instance_id` = #{parentInstanceId}</if>
            <if test="startAt != null">AND `created_at` >= #{startAt}</if>
            <if test="endAt != null">AND `created_at` &lt;= #{endAt}</if>
        </where>
    </sql>
    <select id="list" resultMap="FlowHistoryInstanceMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        <include refid="criteria"/>
        <if test="orderBy != null">ORDER BY ${orderBy}</if>
        <if test="desc != null">DESC</if>
    </select>

    <!-- 计数 -->
    <select id="count" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM
        <include refid="tb"/>
        <include refid="criteria"/>
    </select>

    <!-- 分页查询 -->
    <select id="paging" parameterType="map" resultMap="FlowHistoryInstanceMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        <include refid="criteria"/>
        <if test="orderBy != null">ORDER BY ${orderBy}</if>
        <if test="desc != null">DESC</if>
        LIMIT #{offset}, #{limit}
    </select>
</mapper>