<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorWarehouseSnapshot">
    <resultMap id="DoctorWarehouseSnapshotMap" type="DoctorWarehouseSnapshot">
        <id column="id" property="id"/>
        <result column="event_id" property="eventId"/>
        <result column="before_event" property="beforeEvent"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        doctor_warehouse_snapshots
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        event_id, before_event, created_at, updated_at
    </sql>

    <sql id="vals">
        #{eventId}, #{beforeEvent}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorWarehouseSnapshot" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findByEventId" parameterType="long" resultMap="DoctorWarehouseSnapshotMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE event_id = #{eventId}
    </select>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <delete id="deleteByEventId" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE event_id = #{eventId}
    </delete>
</mapper>