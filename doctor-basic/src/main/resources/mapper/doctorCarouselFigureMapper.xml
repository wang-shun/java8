<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorCarouselFigure">
    <resultMap id="DoctorCarouselFigureMap" type="DoctorCarouselFigure">
        <result column="index" property="index"/>
        <result column="status" property="status"/>
        <result column="url" property="url"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        doctor_carousel_figures
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        index, status, url, created_at, updated_at
    </sql>

    <sql id="vals">
        #{index}, #{status}, #{url}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorCarouselFigure" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorCarouselFigureMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorCarouselFigure">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="index != null">index = #{index},</if>
            <if test="status != null">status = #{status},</if>
            <if test="url != null">url = #{url},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="findByStatus" parameterType="int" resultMap="DoctorCarouselFigureMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        <where>
            <if test="status != null">status = #{status}</if>
        </where>
    </select>
</mapper>