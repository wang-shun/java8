<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorMaterialInWareHouse">

    <resultMap id="DoctorMaterialInWareHouseMap" type="DoctorMaterialInWareHouse">

        <result column="id" property="id"/>
        <result column="farm_id" property="farmId"/>
        <result column="farm_name" property="farmName"/>
        <result column="ware_house_id" property="wareHouseId"/>
        <result column="ware_house_name" property="wareHouseName"/>
        <result column="material_id" property="materialId"/>
        <result column="material_name" property="materialName"/>
        <result column="type" property="type"/>
        <result column="lot_number" property="lotNumber"/>
        <result column="unit_group_name" property="unitGroupName"/>
        <result column="unit_name" property="unitName"/>
        <result column="extra" property="extra"/>
        <result column="creator_id" property="creatorId"/>
        <result column="creator_name" property="creatorName"/>
        <result column="updator_id" property="updatorId"/>
        <result column="updator_name" property="updatorName"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    
    </resultMap>

    <sql id="tb"> doctor_material_in_ware_houses </sql>

    <sql id="cols_all">
      id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        `farm_id`,`farm_name`,`ware_house_id`,`ware_house_name`,`material_id`,`material_name`,`type`,`lot_number`,`unit_group_name`,`unit_name`,`extra`,`creator_id`,`creator_name`,`updator_id`,`updator_name`,`created_at`,`updated_at`
    </sql>

    <sql id="vals">
        #{farmId},#{farmName},#{wareHouseId},#{wareHouseName},#{materialId},#{materialName},#{type},#{lotNumber},#{unitGroupName},#{unitName},#{extra},#{creatorId},#{creatorName},#{updatorId},#{updatorName},now(),now()
    </sql>

    <sql id="criteria">
        <if test="farmId != null" > AND `farm_id` = #{farmId}</if>
        <if test="wareHouseId != null" > AND `ware_house_id` = #{wareHouseId}</if>
        <if test="materialId != null" > AND `material_id` = #{materialId}</if>
        <if test="materialName != null" > AND `material_name` LIKE concat('%',#{materialName},'%')</if>
        <if test="type!=null">AND  `type`=#{type}</if>
    </sql>

    <insert id="create" parameterType="DoctorMaterialInWareHouse" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO <include refid="tb" />
        (<include refid="cols_exclude_id" />)
            VALUES (<include refid="vals" />)
    </insert>

    <insert id="creates" parameterType="list">
        INSERT INTO <include refid="tb"/>
        ( <include refid="cols_exclude_id"/> )
        VALUES
        <foreach collection="list" item="i" separator=",">
        (#{i.farmId},#{i.farmName},#{i.wareHouseId},#{i.wareHouseName},#{i.materialId},#{i.materialName},#{type},#{i.lotNumber},#{i.unitGroupName},#{i.unitName},#{i.extra},#{i.creatorId},#{i.creatorName},#{i.updatorId},#{i.updatorName},now(),now())
        </foreach>
    </insert>

    <delete id="delete" parameterType="Long">
        DELETE FROM <include refid="tb"/> WHERE
        id=#{id}
    </delete>

    <select id="list" resultMap="DoctorMaterialInWareHouseMap">
        SELECT <include refid="cols_all"/> FROM <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
    </select>

    <select id="findById" parameterType="long" resultMap="DoctorMaterialInWareHouseMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <select id="queryByFarmMaterial" parameterType="map" resultMap="DoctorMaterialInWareHouseMap">
        SELECT <include refid="cols_all"/> from <include refid="tb"/> WHERE
        farm_id=#{farmId} AND material_id=#{materialId}
    </select>

    <select id="queryByFarmAndWareHouseId" parameterType="map" resultMap="DoctorMaterialInWareHouseMap">
        SELECT <include refid="cols_all"/> from <include refid="tb"/>
        WHERE farm_id = #{farmId} and ware_house_id = #{wareHouseId}
    </select>

    <select id="queryByFarmType" parameterType="map" resultMap="DoctorMaterialInWareHouseMap">
        SELECT <include refid="cols_all"/> from <include refid="tb"/> WHERE
          farm_id=#{farmId} and `type`=#{type};
    </select>

    <select id="queryByFarmHouseMaterial" parameterType="map" resultMap="DoctorMaterialInWareHouseMap">
        SELECT <include refid="cols_all"/> from <include refid="tb"/> WHERE
          farm_id=#{farmId} AND ware_house_id=#{wareHouseId} AND material_id=#{materialId}
    </select>

    <select id="count" resultType="long">
      SELECT count(1) FROM <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
    </select>

    <select id="paging" resultMap="DoctorMaterialInWareHouseMap">
        SELECT <include refid="cols_all"/> FROM <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
        order by case `lot_number` when 0 then 0 else 1 end desc, updated_at desc
        LIMIT #{offset}, #{limit}
    </select>

    <update id="updateMaterialInfoByMaterialId" parameterType="map">

        UPDATE <include refid="tb"/>
        <set>
            updated_at = now()
            <if test="materialName != null" > ,`material_name` = #{materialName}</if>
            <if test="unitGroupName != null" > ,`unit_group_name` = #{unitGroupName}</if>
            <if test="unitName != null" > ,`unit_name` = #{unitName}</if>
        </set>
        WHERE  `material_id` = #{materialId}
    </update>

    <update id="update" parameterType="DoctorMaterialInWareHouse">
        UPDATE <include refid="tb"/>
        <set>
        updated_at = now()
        <if test="farmId != null" > ,`farm_id` = #{farmId}</if>
        <if test="farmName != null" > ,`farm_name` = #{farmName}</if>
        <if test="wareHouseId != null" > ,`ware_house_id` = #{wareHouseId}</if>
        <if test="wareHouseName != null" > ,`ware_house_name` = #{wareHouseName}</if>
        <if test="materialId != null" > ,`material_id` = #{materialId}</if>
        <if test="materialName != null" > ,`material_name` = #{materialName}</if>
        <if test="type!=null">, `type`=#{type}</if>
        <if test="lotNumber != null" > ,`lot_number` = #{lotNumber}</if>
        <if test="unitGroupName != null" > ,`unit_group_name` = #{unitGroupName}</if>
        <if test="unitName != null" > ,`unit_name` = #{unitName}</if>
        <if test="extra != null" > ,`extra` = #{extra}</if>
        <if test="creatorId != null" > ,`creator_id` = #{creatorId}</if>
        <if test="creatorName != null" > ,`creator_name` = #{creatorName}</if>
        <if test="updatorId != null" > ,`updator_id` = #{updatorId}</if>
        <if test="updatorName != null" > ,`updator_name` = #{updatorName}</if>
        <if test="createdAt != null" > ,`created_at` = #{createdAt}</if>
        </set>
        WHERE id=#{id}
    </update>
    <select id="findMaterialUnit" parameterType="map" resultMap="DoctorMaterialInWareHouseMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE 1 = 1
        <if test="farmId != null"> AND farm_id = #{farmId}</if>
        <if test="materialId != null"> AND material_id = #{materialId}</if>
        <if test="wareHouseId != null">AND ware_house_id = #{wareHouseId}</if>
    </select>

</mapper>