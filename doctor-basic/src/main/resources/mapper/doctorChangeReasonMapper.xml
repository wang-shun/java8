<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorChangeReason">
    <resultMap id="DoctorChangeReasonMap" type="DoctorChangeReason">
        <id column="id" property="id"/>
        <result column="srm" property="srm"/>
        <result column="change_type_id" property="changeTypeId"/>
        <result column="reason" property="reason"/>
        <result column="out_id" property="outId"/>
        <result column="extra" property="extra"/>
        <result column="creator_id" property="creatorId"/>
        <result column="creator_name" property="creatorName"/>
        <result column="updator_id" property="updatorId"/>
        <result column="updator_name" property="updatorName"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        doctor_change_reasons
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        srm, change_type_id, reason, out_id, extra, creator_id, creator_name, updator_id, updator_name, created_at, updated_at
    </sql>

    <sql id="vals">
        #{srm}, #{changeTypeId}, #{reason}, #{outId}, #{extra}, #{creatorId}, #{creatorName}, #{updatorId}, #{updatorName}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorChangeReason" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorChangeReasonMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorChangeReason">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="srm != null">srm = #{srm},</if>
            <if test="changeTypeId != null">change_type_id = #{changeTypeId},</if>
            <if test="reason != null">reason = #{reason},</if>
            <if test="outId != null">out_id = #{outId},</if>
            <if test="extra != null">extra = #{extra},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="creatorName != null">creator_name = #{creatorName},</if>
            <if test="updatorId != null">updator_id = #{updatorId},</if>
            <if test="updatorName != null">updator_name = #{updatorName},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="findByChangeTypeId" parameterType="Long" resultMap="DoctorChangeReasonMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE change_type_id = #{changeTypeId}
    </select>

    <select id="findBySrm" parameterType="string" resultMap="DoctorChangeReasonMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE srm like concat('%', #{srm}, '%')
    </select>
</mapper>