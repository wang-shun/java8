<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorBasic">
    <resultMap id="DoctorBasicMap" type="DoctorBasic">
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="type_name" property="typeName"/>
        <result column="is_valid" property="isValid"/>
        <result column="srm" property="srm"/>
        <result column="context" property="context"/>
        <result column="out_id" property="outId"/>
        <result column="extra" property="extra"/>
        <result column="updator_id" property="updatorId"/>
        <result column="updator_name" property="updatorName"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>

    </resultMap>

    <sql id="tb">
        doctor_basics
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        name, type, type_name, is_valid, srm, context, out_id, extra, updator_id, updator_name, created_at, updated_at
    </sql>

    <sql id="vals">
        #{name}, #{type}, #{typeName}, #{isValid}, #{srm}, #{context}, #{outId}, #{extra}, #{updatorId}, #{updatorName}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorBasic" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorBasicMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <select id="findByType" parameterType="int" resultMap="DoctorBasicMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE type = #{type} AND is_valid = 1
    </select>

    <update id="update" parameterType="DoctorBasic">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="type != null">type = #{type},</if>
            <if test="typeName != null">type_name = #{typeName},</if>
            <if test="isValid != null">is_valid = #{isValid},</if>
            <if test="srm != null">srm = #{srm},</if>
            <if test="context != null">context = #{context},</if>
            <if test="outId != null">out_id = #{outId},</if>
            <if test="extra != null">extra = #{extra},</if>
            <if test="updatorId != null">updator_id = #{updatorId},</if>
            <if test="updatorName != null">updator_name = #{updatorName},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

</mapper>