<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="UserExtend">

    <sql id="tb">
        parana_users
    </sql>

    <sql id="cols_all">
        id,
        <include refid="cols_exclude_id"/>
    </sql>

    <sql id="cols_exclude_id">
        name, email, mobile, password, type, status, roles_json, extra_json, tags_json, created_at, updated_at
    </sql>

    <sql id="criteria">
        WHERE 1 = 1
        <if test="name != null"> AND name LIKE CONCAT('%', #{name}, '%')</if>
        <if test="email != null"> AND email = #{email}</if>
        <if test="mobile != null"> AND mobile = #{mobile}</if>
        <if test="type != null"> AND type = #{type}</if>
        <if test="type == null and types != null">
            AND `type` IN
            <foreach collection="types" item="type"
                     open="(" separator="," close=")">
                #{type}
            </foreach>
        </if>
        <if test="status != null"> AND status = #{status}</if>
        <!-- 下面是一些高级筛选 -->
        <if test="login!= null">AND `name` = #{login}</if><!-- 登录名完全匹配 -->
        <if test="displayName != null"> AND name LIKE CONCAT(#{displayName}, '%')</if><!-- 昵称模糊匹配 -->
        <if test="displayNameExact != null"> AND name = #{displayNameExact}</if><!-- 昵称完全匹配 -->
        <if test="haveMobile != null"> AND mobile is not null AND  mobile !=""</if> <!-- 手机号不为空 -->
        <if test="searchValue!= null"> AND (`name` = #{searchValue} or email = #{searchValue} or mobile = #{searchValue})</if> <!-- 手机号、邮箱、登录名匹配-->
    </sql>

    <select id="list" resultMap="UserMap" parameterType="map">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        <include refid="criteria" />
        <if test="ids != null">
            AND id IN
            <foreach collection="ids" open="(" separator="," close=")" item="id">
                #{id}
            </foreach>
        </if>
    </select>

    <select id="maxId" resultType="long">
        select max(id) from <include refid="tb"/>
    </select>

    <select id="minDate" resultType="date">
        SELECT min(updated_at) FROM <include refid="tb"/>
    </select>

    <!-- 计数 -->
    <select id="count" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM <include refid="tb"/>
        <include refid="criteria"/>
    </select>

    <!-- 分页查询 -->
    <select id="paging" parameterType="map" resultMap="UserMap">
        SELECT
        <include refid="cols_all"/>
        FROM <include refid="tb"/>
        <include refid="criteria"/>
        ORDER BY id DESC LIMIT #{offset}, #{limit}
    </select>

    <update id="batchUpdateRoles" parameterType="map">
        UPDATE
        <include refid="tb"/>
        SET
        roles_json = #{rolesJson},
        updated_at= now()
        WHERE id in
        <foreach collection="userIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>
</mapper>