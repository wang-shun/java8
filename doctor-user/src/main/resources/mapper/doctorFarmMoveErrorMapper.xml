<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorFarmMoveError">
    <resultMap id="DoctorFarmMoveErrorMap" type="DoctorFarmMoveError">
        <id column="id" property="id"/>
        <result column="farm_name" property="farmName"/>
        <result column="code" property="code"/>
        <result column="type" property="type"/>
        <result column="out_id" property="outId"/>
        <result column="event_name" property="eventName"/>
        <result column="event_at" property="eventAt"/>
        <result column="event_out_id" property="eventOutId"/>
        <result column="error" property="error"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        doctor_farm_move_errors
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        farm_name, code, type, out_id, event_name, event_at, event_out_id, error, created_at, updated_at
    </sql>

    <sql id="vals">
        #{farmName}, #{code}, #{type}, #{outId}, #{eventName},#{eventAt}, #{eventOutId}, #{error}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorFarmMoveError" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <delete id="deleteByType" >
        DELETE FROM <include refid="tb" />
        WHERE `type` = #{type}
    </delete>
</mapper>