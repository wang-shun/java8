<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorFarm">
    <resultMap id="DoctorFarmMap" type="DoctorFarm">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="farm_code" property="farmCode"/>
        <result column="org_id" property="orgId"/>
        <result column="org_name" property="orgName"/>
        <result column="province_id" property="provinceId"/>
        <result column="province_name" property="provinceName"/>
        <result column="city_id" property="cityId"/>
        <result column="city_name" property="cityName"/>
        <result column="district_id" property="districtId"/>
        <result column="district_name" property="districtName"/>
        <result column="detail_address" property="detailAddress"/>
        <result column="out_id" property="outId"/>
        <result column="extra" property="extra"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        doctor_farms
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        name, farm_code, org_id, org_name, province_id, province_name, city_id, city_name, district_id, district_name, detail_address, out_id, extra, created_at, updated_at
    </sql>

    <sql id="vals">
        #{name}, #{farmCode}, #{orgId}, #{orgName}, #{provinceId}, #{provinceName}, #{cityId}, #{cityName}, #{districtId}, #{districtName}, #{detailAddress}, #{outId}, #{extra}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorFarm" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <insert id="creates" parameterType="list" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        <foreach collection="list" separator="," item="i">
            (#{i.name}, #{i.farmCode}, #{i.orgId}, #{i.orgName}, #{i.provinceId}, #{i.provinceName}, #{i.cityId}, #{i.cityName}, #{i.districtId}, #{i.districtName}, #{i.detailAddress}, #{i.outId}, #{i.extra}, now(), now())
        </foreach>
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorFarmMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <select id="findByOutId" parameterType="string" resultMap="DoctorFarmMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE out_id = #{outId}
    </select>

    <select id="findByIds" parameterType="list" resultMap="DoctorFarmMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id in
        <foreach collection="list" open="(" separator="," close=")" item="id">
            #{id}
        </foreach>
    </select>

    <select id="findByOrgId" parameterType="long" resultMap="DoctorFarmMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE org_id = #{orgId}
    </select>

    <update id="update" parameterType="DoctorFarm">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="farmCode != null">farm_code = #{farmCode},</if>
            <if test="orgId != null">org_id = #{orgId},</if>
            <if test="orgName != null">org_name = #{orgName},</if>
            <if test="provinceId != null">province_id = #{provinceId},</if>
            <if test="provinceName != null">province_name = #{provinceName},</if>
            <if test="cityId != null">city_id = #{cityId},</if>
            <if test="cityName != null">city_name = #{cityName},</if>
            <if test="districtId != null">district_id = #{districtId},</if>
            <if test="districtName != null">district_name = #{districtName},</if>
            <if test="detailAddress != null">detail_address = #{detailAddress},</if>
            <if test="outId != null">out_id = #{outId},</if>
            <if test="extra != null">extra = #{extra},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="findAll" resultMap="DoctorFarmMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
    </select>
</mapper>