<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="UserBind">
    <resultMap id="UserBindMap" type="UserBind">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="target_system" property="targetSystem"/>
        <result column="uuid" property="uuid"/>
        <result column="target_user_name" property="targetUserName"/>
        <result column="target_user_mobile" property="targetUserMobile"/>
        <result column="target_user_email" property="targetUserEmail"/>
        <result column="extra" property="extra"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        doctor_user_binds
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        user_id, target_system, uuid, target_user_name, target_user_mobile, target_user_email, extra, created_at, updated_at
    </sql>

    <sql id="vals">
        #{userId}, #{targetSystem}, #{uuid}, #{targetUserName}, #{targetUserMobile}, #{targetUserEmail}, #{extra}, now(), now()
    </sql>

    <insert id="create" parameterType="UserBind" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="UserBindMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>


    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="findByUserId" resultMap="UserBindMap" parameterType="long">
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE user_id = #{userId}
    </select>

    <select id="findUnique" resultMap="UserBindMap" parameterType="map">
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE user_id = #{userId} and target_system = #{targetSystem}
    </select>

    <update id="update" parameterType="UserBind">
        update <include refid="tb" />
        <set>
            <if test="userId != null"> user_id = #{userId}, </if>
            <if test="targetSystem != null"> target_system = #{targetSystem}, </if>
            <if test="uuid != null"> uuid = #{uuid}, </if>
            <if test="targetUserName != null"> target_user_name = #{targetUserName}, </if>
            <if test="targetUserMobile != null"> target_user_mobile = #{targetUserMobile}, </if>
            <if test="targetUserEmail != null"> target_user_email = #{targetUserEmail}, </if>
            <if test="extra != null"> extra = #{extra}, </if>
            updated_at = now()
        </set>
        where id = #{id}
    </update>
</mapper>