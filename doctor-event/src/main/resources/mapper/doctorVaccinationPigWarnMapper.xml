<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorVaccinationPigWarn">
    <resultMap id="DoctorVaccinationPigWarnMap" type="DoctorVaccinationPigWarn">
        <id column="id" property="id"/>
        <result column="farm_id" property="farmId"/>
        <result column="farm_name" property="farmName"/>
        <result column="pig_type" property="pigType"/>
        <result column="material_id" property="materialId"/>
        <result column="material_name" property="materialName"/>
        <result column="start_date" property="startDate"/>
        <result column="end_date" property="endDate"/>
        <result column="vaccination_date_type" property="vaccinationDateType"/>
        <result column="input_value" property="inputValue"/>
        <result column="input_date" property="inputDate"/>
        <result column="dose" property="dose"/>
        <result column="remark" property="remark"/>
        <result column="creator_id" property="creatorId"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        doctor_vaccination_pig_warns
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        farm_id, farm_name, pig_type, material_id, material_name, start_date, end_date, vaccination_date_type, input_value, input_date, dose, remark, `creator_id`, `created_at`,`updated_at`
    </sql>

    <sql id="vals">
        #{farmId}, #{farmName}, #{pigType}, #{materialId}, #{materialName}, #{startDate}, #{endDate}, #{vaccinationDateType}, #{inputValue}, #{inputDate}, #{dose}, #{remark}, #{creatorId}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorVaccinationPigWarn" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorVaccinationPigWarnMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorVaccinationPigWarn">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="farmId != null">farm_id = #{farmId},</if>
            <if test="farmName != null">farm_name = #{farmName},</if>
            <if test="pigType != null">pig_type = #{pigType},</if>
            <if test="materialId != null">material_id = #{materialId},</if>
            <if test="materialName != null">material_name = #{materialName},</if>
            <if test="startDate != null">start_date = #{startDate},</if>
            <if test="endDate != null">end_date = #{endDate},</if>
            <if test="vaccinationDateType != null">vaccination_date_type = #{vaccinationDateType},</if>
            <if test="inputValue != null">input_value = #{inputValue},</if>
            <if test="inputDate != null">input_date = #{inputDate},</if>
            <if test="dose != null">dose = #{dose},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="creatorId != null">`creator_id` = #{creatorId},</if>
            <if test="createdAt != null">`created_at` = #{createdAt},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <sql id="criteria">
        <if test="farmId != null">AND farm_id = #{farmId}</if>
        <if test="pigType != null">AND pig_type = #{pigType}</if>
        <if test="materialId != null">AND material_id = #{materialId}</if>
        <if test="vaccinationDateType != null">AND vaccination_date_type = #{vaccinationDateType}</if>
        <if test="creatorId != null">AND creator_id = #{creatorId}</if>
    </sql>

    <select id="list" parameterType="map" resultMap="DoctorVaccinationPigWarnMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
    </select>

    <select id="count" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
    </select>

    <select id="paging" parameterType="map" resultMap="DoctorVaccinationPigWarnMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
        ORDER BY `id` DESC
        LIMIT #{offset}, #{limit}
    </select>
</mapper>