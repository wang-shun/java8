<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorMessageRuleTemplate">
    <resultMap id="DoctorMessageRuleTemplateMap" type="DoctorMessageRuleTemplate">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="category" property="category"/>
        <result column="rule_value" property="ruleValue"/>
        <result column="status" property="status"/>
        <result column="message_template" property="messageTemplate"/>
        <result column="content" property="content"/>
        <result column="producer" property="producer"/>
        <result column="describe" property="describe"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="updated_by" property="updatedBy"/>
        
    </resultMap>

    <sql id="tb">
        doctor_message_rule_templates
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        name, type, category, rule_value, status, message_template, content, producer, `describe`, created_at, updated_at, updated_by
    </sql>

    <sql id="vals">
        #{name}, #{type}, #{category}, #{ruleValue}, #{status}, #{messageTemplate}, #{content}, #{producer}, #{describe}, now(), now(), #{updatedBy}
    </sql>

    <insert id="create" parameterType="DoctorMessageRuleTemplate" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorMessageRuleTemplateMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorMessageRuleTemplate">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="type != null">type = #{type},</if>
            <if test="category != null">category = #{category},</if>
            <if test="ruleValue != null">rule_value = #{ruleValue},</if>
            <if test="status != null">status = #{status},</if>
            <if test="messageTemplate != null">message_template = #{messageTemplate},</if>
            <if test="content != null">content = #{content},</if>
            <if test="producer != null">producer = #{producer},</if>
            <if test="describe != null">`describe` = #{describe},</if>
            <if test="updatedBy != null">updated_by = #{updatedBy},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <sql id="criteria">
        <if test="name != null">AND = #{name}</if>
        <if test="type != null">AND type = #{type}</if>
        <if test="category != null">AND category = #{category}</if>
        <if test="status != null">AND status = #{status}</if>
        <if test="messageTemplate != null">AND message_template = #{messageTemplate}</if>
        <if test="producer != null">AND producer = #{producer}</if>
        <if test="types != null &amp;&amp; types.size() > 0">
            AND `type` IN
            <foreach collection="types" item="i" open="(" separator="," close=")">
                #{i}
            </foreach>
        </if>
    </sql>

    <select id="list" parameterType="map" resultMap="DoctorMessageRuleTemplateMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
    </select>

    <select id="listNotDelete" parameterType="map" resultMap="DoctorMessageRuleTemplateMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        <where>
            `status` != -1
            <include refid="criteria"/>
        </where>
    </select>

    <!-- 根据消息种类获取 -->
    <select id="findByCategory" parameterType="int" resultMap="DoctorMessageRuleTemplateMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE category = #{category} AND `status` != -1
    </select>

    <!-- 获取所有的预警和警报消息 -->
    <select id="findAllWarnMessageTpl" resultMap="DoctorMessageRuleTemplateMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE (`type` = 1 OR `type` = 2) AND `status` != -1
    </select>

</mapper>