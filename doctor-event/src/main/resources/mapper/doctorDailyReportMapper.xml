<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorDailyReport">
    <resultMap id="DoctorDailyReportMap" type="DoctorDailyReport">
        <result column="farm_id" property="farmId"/>
        <result column="farm_name" property="farmName"/>
        <result column="sow_count" property="sowCount"/>
        <result column="farrow_count" property="farrowCount"/>
        <result column="nursery_count" property="nurseryCount"/>
        <result column="fatten_count" property="fattenCount"/>
        <result column="data" property="data"/>
        <result column="extra" property="extra"/>
        <result column="sum_at" property="sumAt"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="tb">
        doctor_daily_reports
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        farm_id,
        farm_name,
        sow_count,
        farrow_count,
        nursery_count,
        fatten_count,
        data,
        extra,
        sum_at,
        created_at
    </sql>

    <sql id="vals">
        #{farmId},
        #{farmName},
        #{sowCount},
        #{farrowCount},
        #{nurseryCount},
        #{fattenCount},
        #{data},
        #{extra},
        #{sumAt},
        now()
    </sql>

    <insert id="create" parameterType="DoctorDailyReport" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <insert id="creates" parameterType="list">
        INSERT INTO <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        <foreach collection="list" separator="," item="item">
            (
            #{item.farmId},
            #{item.farmName},
            #{item.sowCount},
            #{item.farrowCount},
            #{item.nurseryCount},
            #{item.fattenCount},
            #{item.data},
            #{item.extra},
            #{item.sumAt},
            now()

            )
        </foreach>
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorDailyReportMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorDailyReport">
        UPDATE <include refid="tb"/>
        <set>
            <if test="farmId != null">farm_id = #{farmId},</if>
            <if test="farmName != null">farm_name = #{farmName},</if>
            <if test="sowCount != null">sow_count = #{sowCount},</if>
            <if test="farrowCount != null">farrow_count = #{farrowCount},</if>
            <if test="nurseryCount != null">nursery_count = #{nurseryCount},</if>
            <if test="fattenCount != null">fatten_count = #{fattenCount},</if>
            <if test="data != null">data = #{data},</if>
            <if test="extra != null">extra = #{extra},</if>
            <if test="sumAt != null">sum_at = #{sumAt},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>
    
    <select id="findByFarmIdAndSumAt" parameterType="map" resultMap="DoctorDailyReportMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE farm_id = #{farmId} AND sum_at = #{sumAt}
        limit 1
    </select>

    <select id="findBySumAt" parameterType="map" resultMap="DoctorDailyReportMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE sum_at = #{sumAt}
    </select>

    <delete id="deleteBySumAt" parameterType="date">
        DELETE FROM <include refid="tb"/>
        WHERE sum_at = #{sumAt}
    </delete>
</mapper>