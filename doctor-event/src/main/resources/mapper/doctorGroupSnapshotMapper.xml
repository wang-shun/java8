<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorGroupSnapshot">
    <resultMap id="DoctorGroupSnapshotMap" type="DoctorGroupSnapshot">
        <id column="id" property="id"/>
        <result column="group_id" property="groupId"/>
        <result column="from_event_id" property="fromEventId"/>
        <result column="to_event_id" property="toEventId"/>
        <result column="to_info" property="toInfo"/>
        <result column="created_at" property="createdAt"/>
        
    </resultMap>

    <sql id="tb">
        doctor_group_snapshots
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        group_id, from_event_id, to_event_id, to_info, created_at
    </sql>

    <sql id="vals">
        #{groupId}, #{fromEventId}, #{toEventId}, #{toInfo}, now()
    </sql>

    <insert id="create" parameterType="DoctorGroupSnapshot" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorGroupSnapshotMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorGroupSnapshot">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="groupId != null">group_id = #{groupId},</if>
            <if test="fromEventId != null">from_event_id = #{fromEventId},</if>
            <if test="toEventId != null">to_event_id = #{toEventId},</if>
            <if test="toInfo != null">to_info = #{toInfo},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="findGroupSnapshotByToEventId" parameterType="long" resultMap="DoctorGroupSnapshotMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE to_event_id = #{toEventId}
    </select>

    <select id="queryByEventId" parameterType="long" resultMap="DoctorGroupSnapshotMap">
        SELECT <include refid="cols_all"/> FROM <include refid="tb"/>
        WHERE to_event_id = (
        SELECT from_event_id
        FROM <include refid="tb"/>
        WHERE to_event_id = #{eventId}
        )
    </select>

    <select id="queryNotSnapshotGroupId" resultType="long" >
        SELECT a.id
        FROM doctor_groups a LEFT JOIN doctor_group_snapshots b on a.id = b.group_id
        WHERE b.group_id IS NULL AND a.status = 1
    </select>

    <select id="queryByFromEventIdIsNull" resultMap="DoctorGroupSnapshotMap" >
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE from_event_id IS NULL
    </select>

    <delete id="deleteByGroupId" parameterType="long">
        delete from <include refid="tb"/>
        where group_id = #{group_id}
    </delete>

</mapper>