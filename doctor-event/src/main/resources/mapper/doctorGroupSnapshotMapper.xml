<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorGroupSnapshot">
    <resultMap id="DoctorGroupSnapshotMap" type="DoctorGroupSnapshot">
        <id column="id" property="id"/>
        <result column="event_type" property="eventType"/>
        <result column="from_group_id" property="fromGroupId"/>
        <result column="to_group_id" property="toGroupId"/>
        <result column="from_event_id" property="fromEventId"/>
        <result column="to_event_id" property="toEventId"/>
        <result column="from_info" property="fromInfo"/>
        <result column="to_info" property="toInfo"/>
        <result column="created_at" property="createdAt"/>
        
    </resultMap>

    <sql id="tb">
        doctor_group_snapshots
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        event_type, from_group_id, to_group_id, from_event_id, to_event_id, from_info, to_info, created_at
    </sql>

    <sql id="vals">
        #{eventType}, #{fromGroupId}, #{toGroupId}, #{fromEventId}, #{toEventId}, #{fromInfo}, #{toInfo}, now()
    </sql>

    <insert id="create" parameterType="DoctorGroupSnapshot" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorGroupSnapshotMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorGroupSnapshot">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="eventType != null">event_type = #{eventType},</if>
            <if test="fromGroupId != null">from_group_id = #{fromGroupId},</if>
            <if test="toGroupId != null">to_group_id = #{toGroupId},</if>
            <if test="fromEventId != null">from_event_id = #{fromEventId},</if>
            <if test="toEventId != null">to_event_id = #{toEventId},</if>
            <if test="fromInfo != null">from_info = #{fromInfo},</if>
            <if test="toInfo != null">to_info = #{toInfo},</if>
            
            
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="findGroupSnapshotByToEventId" parameterType="long" resultMap="DoctorGroupSnapshotMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE to_event_id = #{toEventId}
    </select>
</mapper>