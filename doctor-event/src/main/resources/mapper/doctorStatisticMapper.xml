<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorStatistic">
    <sql id="pigCriteria">
        AND status = 1
        AND event_source != 5
        <if test="farmIds != null &amp;&amp; farmIds.size() > 0">
            AND farm_id IN
            <foreach collection="farmIds" item="fId" open="(" separator="," close=")">
                #{fId}
            </foreach>
        </if>
        <if test="farmId != null">AND farm_id = #{farmId}</if>
        <if test="startAt != null">AND event_at &gt;= #{startAt}</if>
        <if test="endAt != null">AND event_at &lt;= #{endAt}</if>
    </sql>

    <sql id="groupCriteria">
        AND status = 1
        AND event_source != 5
        <if test="type != null">AND type = #{type}</if>
        <if test="startAt != null">AND event_at &gt;= #{startAt}</if>
        <if test="endAt != null">AND event_at &lt;= #{endAt}</if>
    </sql>


</mapper>