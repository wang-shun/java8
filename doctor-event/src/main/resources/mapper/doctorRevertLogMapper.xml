<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorRevertLog">
    <resultMap id="DoctorRevertLogMap" type="DoctorRevertLog">
        <id column="id" property="id"/>
        <result column="type" property="type"/>
        <result column="from_info" property="fromInfo"/>
        <result column="to_info" property="toInfo"/>
        <result column="reverter_id" property="reverterId"/>
        <result column="reverter_name" property="reverterName"/>
        <result column="created_at" property="createdAt"/>
        
    </resultMap>

    <sql id="tb">
        doctor_revert_logs
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        type, from_info, to_info, reverter_id, reverter_name, created_at
    </sql>

    <sql id="vals">
        #{type}, #{fromInfo}, #{toInfo}, #{reverterId}, #{reverterName}, now()
    </sql>

    <insert id="create" parameterType="DoctorRevertLog" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorRevertLogMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorRevertLog">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="type != null">type = #{type},</if>
            <if test="fromInfo != null">from_info = #{fromInfo},</if>
            <if test="toInfo != null">to_info = #{toInfo},</if>
            <if test="reverterId != null">reverter_id = #{reverterId},</if>
            <if test="reverterName != null">reverter_name = #{reverterName},</if>
            
            
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>
    
</mapper>