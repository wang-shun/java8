<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorPigTrack">

    <resultMap id="DoctorPigTrackMap" type="DoctorPigTrack">
        <id column="id" property="id"/>
        <result column="farm_id" property="farmId"/>
        <result column="pig_id" property="pigId"/>
        <result column="pig_type" property="pigType"/>
        <result column="status" property="status"/>
        <result column="is_removal" property="isRemoval"/>
        <result column="current_barn_id" property="currentBarnId"/>
        <result column="current_barn_name" property="currentBarnName"/>
        <result column="current_barn_type" property="currentBarnType"/>
        <result column="weight" property="weight"/>
        <result column="group_id" property="groupId"/>
        <result column="farrow_qty" property="farrowQty"/>
        <result column="unwean_qty" property="unweanQty"/>
        <result column="wean_qty" property="weanQty"/>
        <result column="farrow_avg_weight" property="farrowAvgWeight"/>
        <result column="wean_avg_weight" property="weanAvgWeight"/>
        <result column="out_farm_date" property="outFarmDate"/>
        <result column="rel_event_ids" property="relEventIds"/>
        <result column="extra" property="extra"/>
        <result column="extra_message" property="extraMessage"/>
        <result column="current_parity" property="currentParity"/>
        <result column="current_mating_count" property="currentMatingCount"/>
        <result column="remark" property="remark"/>
        <result column="creator_id" property="creatorId"/>
        <result column="creator_name" property="creatorName"/>
        <result column="updator_id" property="updatorId"/>
        <result column="updator_name" property="updatorName"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    
    <resultMap id="DoctorPigStatusCountMap" type="DoctorPigStatusCount">
        <result column="status" property="status"/>
        <result column="count" property="count"/>
    </resultMap>

    <sql id="tb"> doctor_pig_tracks </sql>

    <sql id="cols_all">
      id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        farm_id,
        pig_id,
        pig_type,
        status,
        is_removal,
        current_barn_id,
        current_barn_name,
        current_barn_type,
        weight,
        group_id,
        farrow_qty,
        unwean_qty,
        wean_qty,
        farrow_avg_weight,
        wean_avg_weight,
        out_farm_date,
        rel_event_ids,
        extra,
        extra_message,
        current_parity,
        current_mating_count,
        remark,
        creator_id,
        creator_name,
        updator_id,
        updator_name,
        created_at,
        updated_at

    </sql>

    <sql id="vals">
        #{farmId},
        #{pigId},
        #{pigType},
        #{status},
        #{isRemoval},
        #{currentBarnId},
        #{currentBarnName},
        #{currentBarnType},
        #{weight},
        #{groupId},
        #{farrowQty},
        #{unweanQty},
        #{weanQty},
        #{farrowAvgWeight},
        #{weanAvgWeight},
        #{outFarmDate},
        #{relEventIds},
        #{extra},
        #{extraMessage},
        #{currentParity},
        #{currentMatingCount},
        #{remark},
        #{creatorId},
        #{creatorName},
        #{updatorId},
        #{updatorName},
        now(),
        now()

    </sql>

    <sql id="criteria">
        <if test="farmId!=null"> AND `farm_id`=#{farmId}</if>
        <if test="pigId != null" > AND `pig_id` = #{pigId}</if>
        <if test="pigType != null" > AND `pig_type` = #{pigType}</if>
        <if test="status != null" > AND `status` = #{status}</if>
        <if test="isRemoval == null"> AND `is_removal` = 0 </if>
        <if test="isRemoval != null"> AND `is_removal` = #{isRemoval}</if>
        <if test="currentBarnId != null" > AND `current_barn_id` = #{currentBarnId}</if>
        <if test="currentBarnType != null" > AND `current_barn_type` = #{currentBarnType}</if>
        <if test="weight != null" > AND `weight` = #{weight}</if>
        <if test="relEventIds != null" > AND `rel_event_ids` = #{relEventIds}</if>
        <if test="currentParity != null" > AND `current_parity` = #{currentParity}</if>
        <if test="currentMatingCount != null"> AND `current_mating_count` = #{currentMatingCount}</if>
    </sql>

    <insert id="create" parameterType="DoctorPigTrack" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO <include refid="tb" />
        (<include refid="cols_exclude_id" />)
            VALUES (<include refid="vals" />)
    </insert>

    <insert id="creates" parameterType="list">
        INSERT INTO <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        <foreach collection="list" separator="," item="item">
            (
            #{item.farmId},
            #{item.pigId},
            #{item.pigType},
            #{item.status},
            #{item.isRemoval},
            #{item.currentBarnId},
            #{item.currentBarnName},
            #{item.currentBarnType},
            #{item.weight},
            #{item.groupId},
            #{item.farrowQty},
            #{item.unweanQty},
            #{item.weanQty},
            #{item.farrowAvgWeight},
            #{item.weanAvgWeight},
            #{item.outFarmDate},
            #{item.relEventIds},
            #{item.extra},
            #{item.extraMessage},
            #{item.currentParity},
            #{item.currentMatingCount},
            #{item.remark},
            #{item.creatorId},
            #{item.creatorName},
            #{item.updatorId},
            #{item.updatorName},
            now(),
            now()
            )
        </foreach>
    </insert>

    <delete id="delete" parameterType="Long">
        DELETE FROM <include refid="tb"/> WHERE
        id=#{id}
    </delete>

    <select id="findById" parameterType="long" resultMap="DoctorPigTrackMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <select id="findByPigIds" parameterType="list" resultMap="DoctorPigTrackMap">
        select <include refid="cols_all"/> from <include refid="tb"/>
        WHERE pig_id in
        <foreach collection="list" close=")" open="(" separator="," item="i">
            #{i}
        </foreach>
    </select>

    <select id="list" resultMap="DoctorPigTrackMap">
        SELECT <include refid="cols_all"/> FROM <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
    </select>

    <select id="findByEventId" parameterType="long" resultMap="DoctorPigTrackMap">
        SELECT <include refid="cols_all"/> FROM <include refid="tb"/>
        WHERE relEventIds = #{relEventIds};
    </select>

    <select id="count" parameterType="map" resultType="long">
        SELECT count(1) FROM <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
    </select>

    <select id="paging" parameterType="map" resultMap="DoctorPigTrackMap">
        SELECT <include refid="cols_all"/> FROM <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
        ORDER BY updated_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countPigTrackByStatus" resultMap="DoctorPigStatusCountMap">
        select `status` as `status`, count(1) as `count` FROM <include refid="tb"/>
        WHERE farm_id = #{farmId}
        group by `status`
    </select>

    <update id="update" parameterType="DoctorPigTrack">
        UPDATE <include refid="tb"/>
        <set>
            <if test="farmId != null">farm_id = #{farmId},</if>
            <if test="pigId != null">pig_id = #{pigId},</if>
            <if test="pigType != null">pig_type = #{pigType},</if>
            <if test="status != null">status = #{status},</if>
            <if test="isRemoval != null">is_removal = #{isRemoval},</if>
            <if test="currentBarnId != null">current_barn_id = #{currentBarnId},</if>
            <if test="currentBarnName != null">current_barn_name = #{currentBarnName},</if>
            <if test="currentBarnType != null">current_barn_Type = #{currentBarnType},</if>
            <if test="weight != null">weight = #{weight},</if>
            <if test="groupId != null &amp;&amp; groupId != -1">group_id = #{groupId},</if>
            <if test="groupId == -1">group_id = NULL,</if>
            <if test="farrowQty != null">farrow_qty = #{farrowQty},</if>
            <if test="unweanQty != null">unwean_qty = #{unweanQty},</if>
            <if test="weanQty != null">wean_qty = #{weanQty},</if>
            <if test="farrowAvgWeight != null">farrow_avg_weight = #{farrowAvgWeight},</if>
            <if test="weanAvgWeight != null">wean_avg_weight = #{weanAvgWeight},</if>
            <if test="outFarmDate != null">out_farm_date = #{outFarmDate},</if>
            <if test="relEventIds != null">rel_event_ids = #{relEventIds},</if>
            <if test="extra != null">extra = #{extra},</if>
            <if test="extraMessage != null">extra_message = #{extraMessage},</if>
            <if test="currentParity != null">current_parity = #{currentParity},</if>
            <if test="currentMatingCount != null">current_mating_count = #{currentMatingCount},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="creatorName != null">creator_name = #{creatorName},</if>
            <if test="updatorId != null">updator_id = #{updatorId},</if>
            <if test="updatorName != null">updator_name = #{updatorName},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateExtraMessage" parameterType="DoctorPigTrack">
        UPDATE <include refid="tb"/>
        <set>
            `extra_message` = #{extraMessage}
        </set>
        WHERE id=#{id}
    </update>

    <!-- search api start -->
    <select id="maxId" resultType="long">
        SELECT MAX(id) FROM
        <include refid="tb"/>
    </select>

    <select id="listSince" resultMap="DoctorPigTrackMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE  <![CDATA[
          id < #{lastId} AND updated_at > #{since}
        ]]>
        ORDER BY id DESC LIMIT #{limit}
    </select>
    <!-- search api end -->

    <select id="findStatusByBarnId" parameterType="long" resultType="int">
        SELECT DISTINCT status FROM <include refid="tb"/>
        WHERE current_barn_id = #{barnId}
    </select>

    <select id="findByBarnId" parameterType="long" resultMap="DoctorPigTrackMap">
        SELECT <include refid="cols_all"/> FROM <include refid="tb"/>
        WHERE current_barn_id = #{barnId}
    </select>

    <select id="findByFarmIdAndStatus" parameterType="map" resultMap="DoctorPigTrackMap">
        SELECT <include refid="cols_all"/> FROM <include refid="tb"/>
        WHERE farm_id = #{farmId} AND `status` = #{status}
    </select>

    <delete id="deleteByFarmId" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE farm_id = #{farmId}
    </delete>

    <update id="updateBoarCurrentParity" parameterType="map">
        UPDATE <include refid="tb"/>
        SET current_parity = #{currentParity}
        WHERE pig_id = #{boarId}
    </update>

    <select id="findFeedSowTrackByGroupId" parameterType="map" resultMap="DoctorPigTrackMap">
        SELECT <include refid="cols_all"/> FROM <include refid="tb"/>
        WHERE group_id = #{groupId} AND `status` = #{status}
    </select>

    <select id="countByPigTypes" parameterType="map" resultType="long">
        SELECT count(1)
        FROM <include refid="tb"/>
        WHERE `current_barn_id` IN
        <foreach collection="barnIds" item="i" open="(" close=")" separator=",">
            #{i}
        </foreach>
    </select>

    <select id="suggestPigs" parameterType="io.terminus.doctor.event.dto.DoctorSuggestPigSearch" resultType="io.terminus.doctor.event.dto.DoctorSuggestPig">
        SELECT p.id, p.pig_code AS pigCode
        FROM doctor_pigs p, doctor_pig_tracks t
        WHERE
        p.id = t.pig_id
        AND t.is_removal = 0
        <if test="sex != null"> AND p.pig_type = #{sex}</if>
        <if test="pigCode != null">AND p.pig_code LIKE concat('%', #{pigCode}, '%')</if>
        <if test="farmId != null"> AND t.farm_id = #{farmId}</if>
        <if test="status != null"> AND t.status = #{status}</if>
        <if test="notStatus != null"> AND t.status != #{notStatus}</if>
        <if test="notMatingCount != null"> AND t.current_mating_count != #{notMatingCount}</if>
        <if test="statuses != null"> AND t.status IN
            <foreach collection="statuses" item="i" open="(" close=")" separator=",">
                #{i}
            </foreach>
        </if>

        <if test="barnTypes != null"> AND t.current_barn_type IN
            <foreach collection="barnTypes" item="i" open="(" close=")" separator=",">
                #{i}
            </foreach>
        </if>
        ORDER BY
        <if test="pigCode != null">
            instr(pig_code, #{pigCode}) ASC, length(pig_code) ASC, pig_code ASC,
        </if>
        pig_code ASC
        limit 20;
    </select>
</mapper>