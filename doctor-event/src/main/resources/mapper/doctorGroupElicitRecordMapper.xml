<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorGroupElicitRecord">
    <resultMap id="DoctorGroupElicitRecordMap" type="DoctorGroupElicitRecord">
        <id column="id" property="id"/>
        <result column="farm_id" property="farmId"/>
        <result column="farm_name" property="farmName"/>
        <result column="group_id" property="groupId"/>
        <result column="group_code" property="groupCode"/>
        <result column="status" property="status"/>
        <result column="from_track" property="fromInfo"/>
        <result column="to_track" property="toInfo"/>
        <result column="error_reason" property="errorReason"/>
        <result column="version" property="version"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="tb">
        doctor_group_elicit_records
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        farm_id,
        farm_name,
        group_id,
        group_code,
        status,
        from_track,
        to_track,
        error_reason,
        version,
        created_at
    </sql>

    <sql id="vals">
        #{farmId},
        #{farmName},
        #{groupId},
        #{groupCode},
        #{status},
        #{fromTrack},
        #{toTrack},
        #{errorReason},
        #{version},
        now()
    </sql>

    <insert id="create" parameterType="DoctorGroupElicitRecord" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorGroupElicitRecordMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <select id="findLastVersion" parameterType="long" resultType="int">
        SELECT ifnull(MAX(version), 0)
        FROM <include refid="tb" />
        WHERE group_id = #{groupId}
    </select>
    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>
    
</mapper>