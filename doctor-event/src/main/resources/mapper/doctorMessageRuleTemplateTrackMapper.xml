<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorMessageRuleTemplateTrack">
    <resultMap id="DoctorMessageRuleTemplateTrackMap" type="DoctorMessageRuleTemplateTrack">
        <id column="id" property="id"/>
        <result column="template_id" property="templateId"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="category" property="category"/>
        <result column="rule_value" property="ruleValue"/>
        <result column="status" property="status"/>
        <result column="message_template" property="messageTemplate"/>
        <result column="content" property="content"/>
        <result column="producer" property="producer"/>
        <result column="describe" property="describe"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="updator_id" property="updatorId"/>
        <result column="updator_name" property="updatorName"/>
        
    </resultMap>

    <sql id="tb">
        doctor_message_rule_template_tracks
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        template_id, `name`, `type`, category, rule_value, status, message_template, content, producer, `describe`, created_at, updated_at, updator_id, updator_name
    </sql>

    <sql id="vals">
        #{templateId}, #{name}, #{type}, #{category}, #{ruleValue}, #{status}, #{messageTemplate}, #{content}, #{producer}, #{describe}, now(), now(), #{updatorId}, #{updatorName}
    </sql>

    <insert id="create" parameterType="DoctorMessageRuleTemplateTrack" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorMessageRuleTemplateTrackMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorMessageRuleTemplateTrack">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="templateId != null">template_id = #{templateId},</if>
            <if test="name != null">`name` = #{name},</if>
            <if test="type != null">`type` = #{type},</if>
            <if test="category != null">category = #{category},</if>
            <if test="ruleValue != null">rule_value = #{ruleValue},</if>
            <if test="status != null">status = #{status},</if>
            <if test="messageTemplate != null">message_template = #{messageTemplate},</if>
            <if test="content != null">content = #{content},</if>
            <if test="producer != null">producer = #{producer},</if>
            <if test="describe != null">`describe` = #{describe},</if>
            
            
            <if test="updatorId != null">updator_id = #{updatorId},</if>
            <if test="updatorName != null">updator_name = #{updatorName},</if>
            
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>
</mapper>