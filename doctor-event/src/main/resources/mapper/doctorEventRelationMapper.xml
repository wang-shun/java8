<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorEventRelation">
    <resultMap id="DoctorEventRelationMap" type="DoctorEventRelation">
        <id column="id" property="id"/>
        <result column="origin_event_id" property="originEventId"/>
        <result column="trigger_event_id" property="triggerEventId"/>
        <result column="trigger_target_type" property="triggerTargetType"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        doctor_event_relations
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
       origin_event_id, trigger_event_id, trigger_target_type, status, created_at, updated_at
    </sql>

    <sql id="vals">
       #{originEventId}, #{triggerEventId}, #{triggerTargetType}, #{status}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorEventRelation" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <insert id="creates" parameterType="list">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        <foreach collection="list" item="i" separator=",">
            (#{i.originEventId}, #{i.triggerEventId},  #{i.triggerTargetType}, #{i.status}, now(), now())
        </foreach>
    </insert>

    <select id="findById" parameterType="long" resultType="DoctorEventRelation" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <select id="findByIds" parameterType="long" resultType="DoctorEventRelation" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id in
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <update id="update" parameterType="DoctorEventRelation">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="originEventId != null">origin_event_id = #{originEventId},</if>
            <if test="triggerEventId != null">trigger_event_id = #{triggerEventId},</if>
            <if test="triggerTargetType != null">trigger_target_type = #{triggerTargetType},</if>
            <if test="status != null">status = #{status},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <delete id="deletes" parameterType="list">
        DELETE FROM <include refid="tb"/>
        WHERE id IN
        <foreach collection="list" item="i" open="(" separator="," close=")">
            #{i}
        </foreach>
    </delete>

    <sql id="criteria">
        <where>
            <if test="originEventId != null">AND origin_event_id = #{originEventId}</if>
            <if test="triggerEventId != null">AND trigger_event_id = #{triggerEventId}</if>
            <if test="triggerTargetType != null">AND trigger_target = #{triggerTargetType}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </sql>

    <select id="list" parameterType="map" resultMap="DoctorEventRelationMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        <include refid="criteria"/>
    </select>

    <select id="count" parameterType="map" resultType="long">
        SELECT COUNT(1)
        FROM <include refid="tb"/>
        <include refid="criteria"/>
    </select>

    <select id="paging" parameterType="map" resultMap="DoctorEventRelationMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        <include refid="criteria"/>
        LIMIT #{offset}, #{limit}
    </select>

    <select id="findByOriginAndType" parameterType="map" resultMap="DoctorEventRelationMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE
        origin_event_id = #{originEventId}
        AND trigger_target_type = #{triggerTargetType}
        AND status = 1
    </select>

    <select id="findByTrigger" parameterType="long" resultMap="DoctorEventRelationMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE
        trigger_event_id = #{triggerEventId}
        AND status = 1
    </select>

    <select id="findByOrigin" parameterType="long" resultMap="DoctorEventRelationMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE
        origin_event_id = #{originEventId}
        AND status = 1
    </select>

    <update id="updateStatusByOrigin" parameterType="map" >
        UPDATE <include refid="tb"/>
        set status = #{status}
        WHERE
        origin_event_id IN
        <foreach collection="list" item="i" open="(" separator="," close=")" >
            #{i}
        </foreach>
    </update>
</mapper>