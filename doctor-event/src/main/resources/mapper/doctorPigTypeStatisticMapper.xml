<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code Generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorPigTypeStatistic">
    <resultMap id="DoctorPigTypeStatisticMap" type="DoctorPigTypeStatistic">
        <result column="org_id" property="orgId"/>
        <result column="farm_id" property="farmId"/>
        <result column="boar" property="boar"/>
        <result column="sow" property="sow"/>
        <result column="farrow" property="farrow"/>
        <result column="nursery" property="nursery"/>
        <result column="fatten" property="fatten"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        doctor_pig_type_statistics
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        org_id, farm_id, boar, sow, farrow, nursery, fatten, created_at, updated_at
    </sql>

    <sql id="vals">
        #{orgId}, #{farmId}, #{boar}, #{sow}, #{farrow}, #{nursery}, #{fatten}, now(), now()
    </sql>

    <insert id="create" parameterType="DoctorPigTypeStatistic" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorPigTypeStatisticMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorPigTypeStatistic">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="boar != null">boar = #{boar},</if>
            <if test="sow != null">sow = #{sow},</if>
            <if test="farrow != null">farrow = #{farrow},</if>
            <if test="nursery != null">nursery = #{nursery},</if>
            <if test="fatten != null">fatten = #{fatten},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>
    
    <select id="findByFarmId" parameterType="long" resultMap="DoctorPigTypeStatisticMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE farm_id = #{farmId}
    </select>

    <select id="findByOrgId" parameterType="long" resultMap="DoctorPigTypeStatisticMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE org_id = #{orgId}
    </select>
</mapper>