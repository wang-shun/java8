<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DoctorParityMonthlyReport">
    <resultMap id="DoctorParityMonthlyReportMap" type="DoctorParityMonthlyReport">
        <id column="id" property="id"/>
        <result column="farm_id" property="farmId"/>
        <result column="parity" property="parity"/>
        <result column="nest_count" property="nestCount"/>
        <result column="percent" property="percent"/>
        <result column="farrow_all" property="farrowAll"/>
        <result column="farrow_avg" property="farrowAvg"/>
        <result column="sum_at" property="sumAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        doctor_parity_monthly_reports
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        farm_id, 
        parity, 
        nest_count, 
        percent, 
        farrow_all, 
        farrow_avg, 
        sum_at, 
        created_at, 
        updated_at
        
    </sql>

    <sql id="vals">
        #{farmId}, 
        #{parity}, 
        #{nestCount}, 
        #{percent}, 
        #{farrowAll}, 
        #{farrowAvg}, 
        #{sumAt}, 
        now(), 
        now()
        
    </sql>

    <sql id="criteria">
        <if test="id != null">AND id = #{id}</if>
        <if test="farmId != null">AND farm_id = #{farmId}</if>
        <if test="parity != null">AND parity = #{parity}</if>
        <if test="nestCount != null">AND nest_count = #{nestCount}</if>
        <if test="percent != null">AND percent = #{percent}</if>
        <if test="farrowAll != null">AND farrow_all = #{farrowAll}</if>
        <if test="farrowAvg != null">AND farrow_avg = #{farrowAvg}</if>
        <if test="sumAt != null">AND sum_at = #{sumAt}</if>
        
        
        
    </sql>

    <insert id="create" parameterType="DoctorParityMonthlyReport" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="DoctorParityMonthlyReportMap" >
        SELECT <include refid="cols_all" /> FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="DoctorParityMonthlyReport">
        UPDATE <include refid="tb"/>
        <set>
            <if test="farmId != null">farm_id = #{farmId},</if>
            <if test="parity != null">parity = #{parity},</if>
            <if test="nestCount != null">nest_count = #{nestCount},</if>
            <if test="percent != null">percent = #{percent},</if>
            <if test="farrowAll != null">farrow_all = #{farrowAll},</if>
            <if test="farrowAvg != null">farrow_avg = #{farrowAvg},</if>
            <if test="sumAt != null">sum_at = #{sumAt},</if>
            
            
            
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="list" parameterType="map" resultMap="DoctorParityMonthlyReportMap">
        SELECT <include refid="cols_all"/> FROM <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
    </select>

    <select id="count" parameterType="map" resultType="long">
        SELECT count(1) FROM <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
    </select>

    <select id="paging" parameterType="map" resultMap="DoctorParityMonthlyReportMap">
        SELECT <include refid="cols_all"/> FROM <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
        LIMIT #{offset}, #{limit}
    </select>


    <insert id="creates" parameterType="list">
        INSERT INTO <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        <foreach collection="list" separator="," item="item">
            (
            #{item.farmId},
            #{item.parity},
            #{item.nestCount},
            #{item.percent},
            #{item.farrowAll},
            #{item.farrowAvg},
            #{item.sumAt},
            now(),
            now()
            )
        </foreach>
    </insert>

    <delete id="deleteBySumAt" parameterType="date">
        DELETE FROM <include refid="tb"/>
        WHERE sum_at = date_format(#{sumAt}, '%Y-%m')
    </delete>

    <delete id="deleteByFarmIdAndSumAt" parameterType="map">
        DELETE FROM <include refid="tb"/>
        WHERE farm_id = #{farmId} AND sum_at = date_format(#{sumAt}, '%Y-%m')
    </delete>

    <sql id="reportCriteria">
        <if test="farmId != null">AND farm_id = #{farmId}</if>
        <if test="startAt != null">AND event_at &gt;= #{startAt}</if>
        <if test="endAt != null">AND event_at &lt;= #{endAt}</if>
    </sql>

    <sql id="reportCriteria2">
        <if test="farmId != null">AND farm_id = #{farmId}</if>
        <if test="startAt != null">AND event_at &gt;= #{startAt}</if>
        <if test="endAt != null">AND event_at &lt;= #{endAt}</if>
        <if test="parityStart != null">AND parity &gt;= #{parityStart}</if>
        <if test="parityEnd != null">AND parity &lt;= #{parityEnd}</if>
    </sql>

    <!-- 生成产仔胎次分析月报 -->
    <select id="constructDoctorParityMonthlyReports" parameterType="map" resultMap="DoctorParityMonthlyReportMap">
        select t1.farm_id,
               t1.parity,
               ifnull(t2.nest_count, 0) as nest_count,
               ifnull(t2.percent, 0) as percent,
               ifnull(t2.farrow_all, 0) as farrow_all,
               ifnull(t2.farrow_avg, 0) as farrow_avg,
               t1.sum_at
        from (
        select
        #{farmId} as farm_id,
        name as parity,
        date_format(#{endAt}, '%Y-%m') as sum_at
        from doctor_basics
        where type = 9
        ) t1 left join
        (
        select
        case when parity &lt; 10 then parity else 10 end as parity,
        ifnull(count(1),0) as nest_count,
        count(1)/(select count(1)
        from doctor_pig_events
        where type = 15
        <include refid="reportCriteria" /> ) as percent,
        ifnull(sum(live_count+dead_count+jx_count+mny_count+black_count),0) as farrow_all,
        ifnull(sum(live_count+dead_count+jx_count+mny_count+black_count),0)/ifnull(count(1),1) as farrow_avg
        from  doctor_pig_events
        where type = 15
        <include refid="reportCriteria" />
        group by farm_id, case when parity &lt; 10 then parity else 10 end
        ) t2 on t1.parity = t2.parity
    </select>


    <!-- 生成产仔胎次分析月报 -->
    <select id="constructDoctorParityMonthlyReportsWithParity" parameterType="map" resultMap="DoctorParityMonthlyReportMap">
        select
        farm_id,
        #{parityStart} as parity,
        ifnull(count(1),0) as nest_count,
        count(1)/(select count(1)
        from doctor_pig_events
        where type = 15
        <include refid="reportCriteria"/> ) as percent,
        ifnull(sum(live_count+dead_count+jx_count+mny_count+black_count),0) as farrow_all,
        ifnull(sum(live_count+dead_count+jx_count+mny_count+black_count),0)/ifnull(count(1),1) as farrow_avg,
        date_format(#{startAt}, '%Y-%m') as sum_at
        from doctor_pig_events
        where type = 15
        <include refid="reportCriteria2" />
        group by farm_id
    </select>

    <!-- 查询当月的胎次分析 -->
    <select id="findDoctorParityMonthlyReports" parameterType="map" resultMap="DoctorParityMonthlyReportMap">
        select <include refid="cols_all"/>
        FROM <include refid="tb"/>
        where farm_id = #{farmId}
        and sum_at = #{sumAt}
    </select>

</mapper>